# rk3588-boot-support

Shared `no_std` primitives independently recovered from identical or structurally identical early-boot code in the RK3588 USBPlug, RAMBOOT, SPL and PCIe images.

This crate contains only behavior supported by multiple binary observations. Image-specific tables and entry points remain in their own mirrored subprojects. It is not a standalone firmware image.
